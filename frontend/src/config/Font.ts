import { StyleSheet } from "react-native";

const Font = StyleSheet.create({
  modalTitle: {
    fontSize: 24,
    fontWeight: "600",
  },
  modalContent: {
    fontSize: 16,
  },
  messages: {
    fontSize: 14,
  },
});

export default Font;
